[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "roho-research"
version = "0.1.0"
description = "Exploratory regression-constrained harness optimization pilot"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["roho*"]

